class WhatchDog(net Clk, net Kick, num Timeout_ms){
  private{
    num Timeout_cycles = round(Clk'frequency * (Timeout_ms*1e-3));
    num N = ceil(log2(Delay_cycles));

    net(N) Count = Timeout_Cycles-1;
  }
  output net Error = 0;

  rtl(Clk, Kick){
    if(Counter) Counter--;
    else        Error = 1;
  }
}
//------------------------------------------------------------------------------

