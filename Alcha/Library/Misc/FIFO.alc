import "Library/Memory/DualPortMemory"
//------------------------------------------------------------------------------

class FIFO(net Clk, net Reset, num Width, num Length){
  private{
    DualPortMemory(Clk, Reset, Width, Length) RAM;

    RAM.RdAddress = 0;
    RAM.WrAddress = 0;
    RAM.WrEnable  = 0;

    num(RAM.RdAddress'width) NumItems = RAM.WrAddress - RAM.RdAddress;
  }

  fsm(Clk, Reset){
    !! FIFO state machine
  }
}
//------------------------------------------------------------------------------

